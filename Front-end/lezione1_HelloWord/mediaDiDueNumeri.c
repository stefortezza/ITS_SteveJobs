#include <stdio.h>

int main()
{
    float a = 3;
    float b = 4;
    float c = 13;

    float media = (a + b + c) / 3;

    printf("La media di %.2f, %.2f e %.2f e': %.2f\n", a, b, c, media);
    return 0;
}