#include <stdio.h>

int main()
{
    // Somma di due numeri interi
    int a = 2;
    int b = 3;
    char sun = "☀️ Sole Emoji";

    // Esegui la somma
    int sum = a + b;

    // Stampa il risultato
    printf("La somma di %d e %d e': %d\n", a, b, sum, sun);
    return 0;
}